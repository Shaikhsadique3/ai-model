services:
  - type: web
    name: churnaizer-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn churnaizer.models.main:app --bind 0.0.0.0:$PORT
    healthCheckPath: /predict
    autoDeploy: true